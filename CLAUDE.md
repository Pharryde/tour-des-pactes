# Tour des Pactes — Contexte projet

Jeu de combat au tour par tour (navigateur). Interface React pilotée par un moteur de combat Rust/WASM.

📖 **Les règles du jeu elles-mêmes sont dans [`REGLES.md`](REGLES.md)** — document lisible rangé par catégorie (tour de combat, actions, combo, armure, esquive, brûlure/poison, étages, Pactes, synergies, bénédictions, XP…). Ce fichier-ci ne garde que les contraintes techniques et les pièges d'implémentation. **Toute modification de règle doit être répercutée dans les deux.**

## 🛠️ COMMANDES ESSENTIELLES

**Installation initiale (ordre obligatoire) :**
```bash
cd moteur_wasm && wasm-pack build --target web && cd ..
npm install
```
(`moteur_wasm` est une dépendance npm locale `file:moteur_wasm/pkg` — le pkg doit exister AVANT `npm install`)

**Développement :**
```bash
npm run dev          # Vite, http://localhost:5173
```

🩺 **`node_modules` se corrompt sur cette machine.** Symptômes : page blanche + `Failed to resolve import "<dépendance>"` en console, ou `ERR_MODULE_NOT_FOUND` au lancement des tests. Signature constante : **les fichiers finissant par `.js` sont absents** alors que leurs voisins `.d.ts`, `.js.map`, `.mjs` et `.html` sont intacts — un `dist/` ne contenant que des `.map` et des `.d.ts` est le tell. Constaté le 2026-08-08 sur `@supabase/*`, puis sur l'arbre de `vitest` (`chai`, `tslib`, `magic-string`…).

⚠️ **Un `npm install` incrémental (sur un `node_modules` existant) peut lui-même provoquer la casse** : c'est ce qui a détruit l'arbre de `vitest`, qui fonctionnait juste avant. Antivirus et cache npm ont été écartés (aucun événement Defender, accès contrôlé aux dossiers et règles ASR désactivés, `npm cache verify` intact) — cause racine non identifiée.

**Réparation fiable** : supprimer `node_modules` **en entier** puis `npm install` (une réinstallation partielle ne suffit pas). Purger aussi `node_modules/.vite`, dont le cache de pré-bundling garde des références vers l'ancien état. **Détection** : scanner les points d'entrée de tous les paquets (`main` de chaque `package.json` doit exister) — un arbre sain donne 0 manquant.

**Après TOUTE modification dans `moteur_wasm/src/*.rs` :**
```bash
npm run wasm
```
Ce script enchaîne le build Rust, la génération des liaisons, **et la purge de `node_modules/.vite`** — sans cette dernière, Vite continue de servir l'ancien moteur depuis son cache de pré-bundling. `node_modules/moteur_wasm` étant une jonction vers `moteur_wasm/pkg`, aucun `npm install` n'est nécessaire ensuite. Pour une simple vérification de compilation sans rebuild : `cd moteur_wasm && cargo check --target wasm32-unknown-unknown`.

🚫 **`wasm-pack` ne fonctionne plus sur cette machine** (constaté le 2026-08-10) : Smart App Control est actif en mode bloquant (`HKLM:\SYSTEM\CurrentControlSet\Control\CI\Policy` → `VerifiedAndReputablePolicyState = 1`) et refuse `wasm-pack.exe`, un binaire non signé portant la marque `Zone.Identifier` (« téléchargé depuis Internet »). Ne pas désactiver Smart App Control pour contourner ça : Windows ne permet pas de le réactiver ensuite sans réinstallation.

Les deux commandes ci-dessus reproduisent exactement ce que faisait wasm-pack. `wasm-bindgen` (CLI) doit être installé à la version EXACTE du `Cargo.lock` — sinon les liaisons générées sont incompatibles :
```bash
cargo install wasm-bindgen-cli --version 0.2.126 --locked
```
Compilée localement, elle n'a pas de marque Internet et passe donc Smart App Control. `pkg/package.json` n'est pas régénéré par `wasm-bindgen` mais il existe déjà et n'a pas à changer.

L'étape `wasm-opt` de wasm-pack est perdue au passage ; elle est compensée par le profil `[profile.release]` de `moteur_wasm/Cargo.toml` (`opt-level = "s"`, `lto`, `codegen-units = 1`, `strip`), qui donne un binaire un peu **plus petit** que l'ancien (132 Ko contre 138 Ko). Ne pas y ajouter `panic = "abort"` : on perdrait les messages de panique que `CombatArene` remonte en console.
⚠️ Sans ce rebuild, le HMR de Vite ne recharge PAS le binaire `.wasm` — le TS compile mais tourne sur l'ancien moteur.

**Sprites générés (`outils/sprites/`) :**
```bash
node outils/sprites/souffleImmobile.mjs --apercu
```
Certaines feuilles de sprites ne viennent pas d'un pack tiers : elles sont **dessinées par un script**, frame par frame, et écrites directement dans `public/sprites/<personnage>/`. `toile.mjs` est la boîte à outils partagée (toile RVBA, polygones, contour automatique, liseré, éclatement en éclats, encodeur PNG via le `zlib` de Node — aucune dépendance npm, pour ne pas exposer le projet à la corruption de `node_modules`). Un fichier par personnage à côté.
⚠️ **Ne jamais retoucher ces PNG à la main** : la prochaine exécution du script les écrase. Le rendu se juge sur les aperçus (`--apercu`, écrits dans `%TEMP%`), dont **`apercu-echelle-jeu.png` est le seul qui compte vraiment** — il montre le sprite à l'échelle réelle du combat (×2), où un détail net à ×8 n'est souvent qu'un pixel gris.
La page `outils/sprites/apercu.html` (servie par Vite en dev, jamais buildée) rejoue toutes les feuilles à la cadence de l'arène : atteindre le boss concerné en jeu pour vérifier une animation coûte une ascension entière.

**Build / qualité / déploiement :**
```bash
npm run build         # tsc -b && vite build
npm run lint           # eslint .
npm test               # vitest run (une passe, PAS de mode watch)
npm run preview        # sert le build de prod en local
```
- Déploiement : Vercel (auto-détecte Vite sur push git, pas de config locale)
- Avant de considérer une tâche terminée : `tsc -b` + `eslint .` + `npm test` propres, `cargo check` si Rust touché, **et test manuel dans le navigateur** (compilation ≠ fonctionnement)
- ⚠️ **Ne jamais lancer de commande longue ou bavarde en avant-plan** (`npm run dev`, `vite`, tout mode `--watch`) : le pseudo-terminal Windows (ConPTY) de cette machine se fige quand un processus écrit en continu, ce qui plante l'application. Lancer en **tâche de fond avec la sortie redirigée vers un fichier log**, puis lire le log — jamais en avant-plan.
- ⚠️ **Ne jamais ouvrir le panneau navigateur intégré (`preview_start`)** : chaque ouverture d'un nouveau panneau a planté l'application (2 occurrences le 2026-08-08). Le serveur Vite, lui, survivait au crash et écoutait toujours après coup — c'est bien le panneau le coupable, pas le serveur ni le port. Corollaire : déplacer le serveur hors de `localhost` ne règle rien et coûte cher, le navigateur intégré refusant en plus ses outils de lecture sur toute origine non-`localhost`.
- **Vérification dans le navigateur** : passer par le vrai Chrome de l'utilisateur (outils `claude-in-chrome`). Serveur dédié en tâche de fond, sur un port distinct de celui de l'utilisateur (5173) :
  ```bash
  npx vite --host 127.0.0.1 --port 5174 --strictPort > /chemin/vers/vite.log 2>&1
  ```
  Le `--host 127.0.0.1` n'est pas décoratif : sans lui Vite n'écoute qu'en IPv6 (`::1`) et `http://127.0.0.1:5174` est injoignable. Le premier `navigate` peut expirer côté outil alors que la navigation a réussi — vérifier avec `tabs_context_mcp` avant de conclure à un échec.

## 🏗️ ARCHITECTURE ET STACK

**Stack :** React 19 + TypeScript + Vite · Rust → WebAssembly (wasm-bindgen) · `localStorage` (source de vérité immédiate) + Supabase (sauvegarde cloud, miroir asynchrone)
Pas de state manager externe, pas de router — état 100% dans un hook custom, écrans gérés par un type union.

⚠️ **Dev local nécessite un `.env.local`** (non commité, voir `.gitignore` — règle `*.local`) avec `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY` et `VITE_TURNSTILE_SITE_KEY`. Attention à l'endroit où ça casse : `npm run dev` démarre **normalement** même sans ces variables (Vite ne les valide pas au démarrage et n'affiche aucune erreur). C'est la **page** qui casse au chargement dans le navigateur — écran blanc + erreur console — parce que `src/utils/supabaseClient.ts` appelle `createClient()` au niveau module. Ne pas chercher l'erreur dans le terminal : elle n'y apparaîtra jamais.

**Dossiers clés :**
- `moteur_wasm/src/` — moteur de combat (source de vérité des calculs)
  - `entite.rs` — structs `Entite`/`StructureEtage` (miroir exact de `src/types.ts`)
  - `combat.rs` — calculs purs : dégâts, combo, esquive
  - `boss_data.rs` — données des étages/monstres/boss (aucune logique, juste des données)
  - `lib.rs` — bindings `wasm_bindgen` exposés au JS (`jouer_tour`, `get_donnees_etages`)
- `src/hooks/useGameState.ts` — **le cerveau de l'app** : tout l'état persistant (`localStorage`) + toute la logique métier (actions, progression, pactes)
- `src/hooks/useSauvegardeCloud.ts` — synchro Supabase isolée (session anonyme, restauration, autosauvegarde) ; ne connaît aucune règle du jeu, seulement QUAND l'état a changé
- `src/hooks/useCombatResume.ts` — reprise d'un combat après rechargement de page. Tout l'état tient dans **une seule** clé `tdp_combat_actif` (un état partiellement réécrit serait de toute façon incohérent) ; passer par `effacerEtatCombat()` plutôt que de manipuler le nom de la clé depuis l'extérieur
- ⚠️ **Le journal de combat est plafonné à `LIMITE_LOGS` (300)** dans `useGameState.ts`, via un `setHistoriqueLogs` qui enveloppe le setter brut — c'est le seul point qui garantit la borne pour la douzaine d'appelants. Sans elle, le coût est quadratique : chaque ligne re-sérialise le tableau entier dans localStorage, ajoute un nœud DOM au journal rendu, et regonfle l'instantané cloud
- `src/components/` — écrans/composants de rendu uniquement (pas de logique métier lourde ; délèguent via callbacks reçus en props)
- `src/utils/` — fonctions pures sans état (pactes, animations, calculs de récompense/compétences) ; contient aussi `supabaseClient.ts` (client singleton), `sauvegardeCloud.ts` (lecture/écriture du snapshot cloud) et `versionApp.ts` (`APP_VERSION`, partagée entre purge locale et rejet cloud)
- `src/utils/*.test.ts` — tests Vitest co-localisés, **fonctions pures uniquement** (aucun JSX/WASM/DOM, d'où une `vitest.config.ts` distincte qui ne charge pas le plugin React). Priorité aux calculs miroirs du moteur Rust, où une divergence rend l'affichage mensonger sans rien casser de visible
- `supabase/migrations/` — schéma SQL de la base Supabase, déployé automatiquement sur `main` via l'intégration GitHub Vercel↔Supabase (pas de CLI/push manuel nécessaire). ⚠️ **L'organisation contient DEUX projets Supabase** : `kmccjfzqakmsjmxyisrq` (le vrai — pointé par `.env.local` ET par le bundle de prod) et `wzeseotszumzkpnkptbn` / `tour-des-pactes-db`, un orphelin créé par le Vercel Marketplace que rien ne référence (d'où sa mise en pause automatique après 7 jours d'inactivité). Vérifier la référence de projet avant de conclure quoi que ce soit sur la sauvegarde cloud — et se rappeler que le vrai projet est soumis à la même règle des 7 jours, sa mise en pause tuant la synchro **en silence** (`console.error` seul, dégradation gracieuse voulue)
- `supabase/analyses.sql` — requêtes d'analyse à coller dans le SQL Editor. **Pas une migration** : jamais exécutée automatiquement, ne modifie rien
- `src/types.ts` — types partagés, DOIT rester en miroir de `entite.rs` (camelCase TS ↔ snake_case Rust via `serde(rename_all = "camelCase")`)
- `public/sprites/<personnage>/` — feuilles de sprites pixel art (une par animation)
- `public/images/` — illustrations de cinématique de boss (une par pacte)

**Pas de pages classiques (Accueil/Tarifs/Contact)** — jeu à écran unique. Navigation = type `Ecran` (union de string literals dans `types.ts`) + rendu conditionnel dans `App.tsx`.

## 📏 CONVENTIONS DE CODE

- **Langue : tout en français** (variables, fonctions, composants, commentaires). Anglais uniquement pour mots-clés techniques/libs.
- Composants : PascalCase (`CombatArene.tsx`). Hooks : camelCase préfixé `use`. Utils : camelCase, un fichier = un domaine.
- **Synchro Rust ↔ TS obligatoire** : toute nouvelle propriété sur `Entite` doit exister des deux côtés (snake_case Rust / camelCase TS).
- **Registre de pactes** : source unique de vérité = `PACTES_REGISTRY` dans `utils/pactes.ts`. Ajouter une entrée suffit à l'activer partout.
- **Nouvel étage** : ajouter `get_etage_xxx()` dans `boss_data.rs` + l'enregistrer dans `get_tous_les_etages()`. Réutiliser les helpers existants (`esquive_std()`, `kit_complet()`, `nom_boss*()`...) plutôt que des littéraux dupliqués.
- **Style** : classes CSS uniquement, tout dans `App.css`/`index.css` (sections commentées par écran). Style inline réservé aux valeurs calculées à l'exécution (ex: largeur de barre de progression). Un seul breakpoint responsive à 768px — toujours vérifier l'absence de débordement horizontal et l'alignement entre cartes symétriques (joueur/monstre) après un changement visuel.
- ⚠️ **Un nouvel écran centré doit être ajouté à la liste d'ID de `index.css`** (`#ecran-hub, #ecran-fin, …`) : `.ecran` ne centre rien par lui-même, et sans cette inscription l'écran hérite d'`align-items: stretch` — les boutons s'étirent d'un bord à l'autre et la page déborde horizontalement. Le symptôme est visuel uniquement, rien ne le signale à la compilation.
- ⚠️ **`.ecran` se dimensionne en `min-height: calc(100dvh - 20px)`, jamais en `height`/`max-height`.** Avec une hauteur imposée, tout contenu plus grand que l'écran est rogné en haut ET en bas sans pouvoir y accéder (les écrans étant centrés verticalement) — c'est ce qui coupait plusieurs écrans sur mobile. Et `dvh` (pas `vh`) parce que `vh` fige la hauteur barres d'outils masquées, donc déborde toujours de la zone réellement visible sur mobile. Corollaire : un enfant ne peut plus compter sur `height: 100%` (le parent n'a plus de hauteur définie) — utiliser `flex: 1`, comme `.arene-wrapper`.
- **React** : jamais de `setState` synchrone dans un `useEffect` (règle ESLint active) — utiliser l'ajustement d'état pendant le rendu quand la condition redevient fausse d'elle-même après mise à jour. `prefer-const` actif.
- **Commentaires** : uniquement pour un "pourquoi" non-évident (contrainte cachée, workaround). Pas de commentaires qui répètent le code.
- **Modèle à respecter pour un nouveau fichier** : une responsabilité claire (composant = rendu, hook = état+logique, util = fonction pure), pas de mélange logique métier / JSX au-delà de l'orchestration.

## 🎯 CONTEXTE ACTUEL (Current Task)

- Intégration d'un système d'animations sprite pixel art (héros + monstre) en 2 temps : action propre du personnage, puis résolution de l'action adverse (`SpriteAnime.tsx`, `utils/animations*.ts`).
- Ajout d'un nouvel étage "Puissance Brute" (monstres sans action Précise) + pacte associé (bonus % dégâts d'Attaque + bonus combo) + règle globale de scaling de puissance des monstres par paliers d'étages pairs.
- Modifications en cours non commitées (voir `git status`) sur le moteur Rust (`boss_data.rs`, `combat.rs`, `entite.rs`) et le TS associé — penser à valider (`cargo check` + `wasm-pack build` + `tsc -b`) avant tout commit.
- Écran de fin enrichi avec les statistiques de la run (étage/record, monstres tués, pactes débloqués, dégâts infligés/bloqués/esquivés) — capturées via `lireValeurPersistante` (localStorage) plutôt que l'état React fermé, pour éviter un piège de closure obsolète sur le tour fatal (cf. `utils/logs.ts`).
- Boss de l'Armure : chance croissante (25/33/50 %) d'enchaîner une Défense après une Défense (`chanceSuiteDefense`), pour finir le tour avec plus d'armure et déclencher plus souvent son effet de fin de tour au lvl2.
- Étage de l'Ombre : `actionsCachees` (bool) remplacé par `actionsVisibles` (nombre, tiré aléatoirement chaque tour) — 3/5 sans pacte, 2/5 face au boss évolué, 1/5 face à sa forme finale. Pacte de l'Ombre II boosté avec Dégâts Précis x2 en plus du blocage d'esquive.
- Nouvel écran "Sortie de la Tour" + combat final contre le Gardien Absolu (agglomérat des 8 Gardiens de Niveau II de la run, PV = nb d'étages × 100, change de forme aléatoirement — sans répétition consécutive — à chaque tour ; voir `utils/megaboss.ts`) avant la victoire totale.
- Arrondi des calculs de dégâts corrigé côté moteur (`.round()` au lieu d'un `as i32` qui tronquait toujours vers zéro) — affectait notamment le Pacte de la Puissance Brute.
- Stat `⚔️` affichée en combat recalculée pour intégrer les bonus % du Pacte de la Puissance Brute (`calculerAttaqueAffichee`) — sinon le joueur ne voyait jamais son bonus contrairement aux pactes à bonus plat (ex: Pacte de la Vie).
- Le Pacte du Combo (multiplicateur) s'applique désormais aussi à la progression du niveau d'Esquive lors d'un enchaînement d'Esquive (auparavant l'Esquive réinitialisait toujours le compteur de combo côté moteur, rendant le pacte inopérant sur cette action).
- Écran d'avertissement dédié affiché à chaque entrée sur un étage PAIR (palier de puissance des monstres, cf. `buffProgressionEtage`), sans détailler les stats concrètes. Ce palier ajoute aussi désormais +10 PV (max et actuels) par palier aux monstres normaux et +20 PV aux boss (peu importe leur forme).
- Validation des points de compétence : `ArbreCompetences.tsx` utilise un brouillon local (`useState`), propagé au jeu (`setCompetences`) uniquement au clic sur "Valider" — abandonner via "Retour au Hub" sans valider annule les changements. Une animation de forgeron (`utils/animationsForge.ts`, sprite `public/sprites/blacksmith/`) se joue 2s après validation : centrée dans l'espace vide entre le bord de l'écran et la colonne de contenu (600px) sur bureau, sous le compteur de points en mobile — échelle ET position calculées à partir de la même variable CSS pour ne jamais chevaucher le contenu.
- Boutons de cet écran alignés sur le vocabulaire partagé du jeu (`.btn-menu` + variante de couleur type `.btn-jouer`/`.btn-danger`) plutôt que des classes propres à l'écran, pour garder la même hauteur/apparence entre tous les boutons d'un même pied de page.
- Pacte du Combo sur l'Esquive : le multiplicateur ne fait plus sauter un palier d'esquive (le niveau reste un décompte fidèle du nombre d'Esquive enchaînées, important pour la décroissance après une autre action). Il amplifie l'écart entre paliers consécutifs à partir du 2e (`paliers_esquive_effectifs()` dans `combat.rs`, mirorée côté TS par `calculerPaliersEsquiveAffiches` pour l'affichage — sinon le % à l'écran restait figé sur les paliers de base, même bug que `calculerAttaqueAffichee`).
- Log de combat explicite quand l'Esquive d'un camp est neutralisée par `bloqueEsquiveOpposant` (Le Vent Mortel forme finale, ou le joueur avec le Pacte de l'Ombre II) : "(Votre esquive est neutralisée !)" / "(L'esquive ennemie est neutralisée !)".
- Le Vent Mortel **forme évoluée** porte `reduction_esquive_opposant = 25` : ses trois formes attaquent l'esquive de plus en plus fort (rien → −25 % → neutralisation). Le champ était jusque-là réservé au joueur (Bénédiction « Regard Hypnotique »), d'où sa reprise dans `appliquerFormeMegaBoss`. ⚠️ La **leçon de mort** du Vent Mortel reste conditionnée à `bloqueEsquiveOpposant` seul : une esquive rabotée se voit encore agir, une esquive annulée non.
- Régénération de PV (10%) recentrée sur le bon boss : L'Anomalie (Pacte de la Vie) régénère tous les 5 tours en forme évoluée, tous les 3 tours en forme finale — ce mécanisme avait été implémenté par erreur sur Chronos (Pacte du Temps), qui n'a que sa perte de PV.
- Zone de Repos : les 5 choix sont disponibles à la 1ère visite d'une run ; ensuite, seuls 3 (tirés aléatoirement à chaque visite, cf. `melangerAleatoirement` exportée de `utils/etages.ts`) restent actifs, les 2 autres sont grisés (`.btn-action:disabled`, en `!important` pour passer devant les couleurs `.bg-*`). Le choix retenu est journalisé avec son effet **mesuré après coup**, jamais avec la valeur du bouton : un soin pris à 96/100 PV ne rend que 4, et un héros intact n'en tire rien du tout — répéter le libellé du menu ferait du récapitulatif un mensonge dans les deux cas.
- Le Gardien Absolu emprunte désormais aussi le pouvoir spécial de sa forme du tour (pas seulement ses stats) : renvoi d'armure, esquive neutralisée, régénération/altération de PV, etc. (`appliquerFormeMegaBoss` dans `utils/megaboss.ts`). Les effets "tous les X tours" sont forcés à un intervalle de 1 pour se déclencher à chaque tour où la forme est active (la forme changeant chaque tour, un intervalle réel de 3-5 tours ne se déclencherait presque jamais). Victoire contre le Gardien Absolu = +10 XP.
- Synergies cachées (`utils/synergies.ts`, source unique de vérité `SYNERGIES_REGISTRY`) : équiper 4 Pactes précis (peu importe leur niveau) au lancement d'une run révèle un bonus de combat secret pour le joueur — `Entite.synergieActive` (enum `Synergie` mirorée Rust/TS). Comme seuls 4 Pactes max peuvent être équipés (3 Niveau I + 1 Niveau II), une seule synergie est active à la fois. Chaque Pacte n'intervient que dans 2 synergies par design.
  - **Guerrier** (Vie/Armure/Force/Temps) : +2 Armure par Attaque programmée, +2 dégâts de base (non permanent, réinitialisé chaque tour) par Défense programmée.
  - **Ninja** (Esquive/Ombre/Combo/Fluidité) : une Esquive réussie arme un Coup Critique (x2) pour la prochaine Précise du même tour.
  - **Tank** (Vie/Armure/Esquive/Fluidité) : une Esquive réussie renvoie l'Armure actuelle en dégâts (absorbés par l'armure adverse comme une Attaque normale) et soigne 10% de cette Armure.
  - **Assassin** (Force/Temps/Ombre/Combo) : Attaque et Précise fusionnent dans la même jauge de combo (`gerer_combo` avec `fusion_ap`), et la Précise bénéficie aussi des bonus du Pacte de la Puissance Brute (normalement réservés à l'Attaque).
  - **Élémentaire** (Foudre/Feu/Poison/Froid) : la brûlure passe par `appliquer_foudre()` (extrait de `calculer_degats` exprès — une action convertie en sort à zéro, donc elle ratait le multiplicateur), et une dose de poison posée sur un créneau froid **en faveur de l'attaquant** (`joueur_dabord` ou action adverse gelée) est doublée. `sur_creneau_froid` est calculé dans `jouer_tour` et passé jusqu'à `convertir_en_poison`.
  - ⚠️ **La clé du registre est un identifiant technique** (sauvegardes `tdp_synergies_decouvertes`, enum Rust `Synergie`) et doit rester sans accent — d'où le champ `nom` pour l'affichage (`Elementaire` en clé, `Élémentaire` à l'écran). Ne jamais afficher la clé brute : passer par `SYNERGIES_REGISTRY[s].nom`.
  - Découverte suivie à vie (`synergiesDecouvertes`, indépendant des stats de run) : 1ère fois = message dramatique + entrée dans les Archives (Tuto) ; ensuite, simple rappel "Synergie active" en début de run.
- `DefinitionAnimation` supporte désormais `frameDebut`/`frameFin` optionnels pour ne jouer qu'un sous-segment d'une feuille de sprites (ex: `attack.png` du mushroom sert à la fois pour l'Esquive — mouvement complet 10 frames — et l'Attaque/la Précise — juste la fin, frames 5-9, le coup de griffe). `frames` doit toujours rester le nombre RÉEL de frames de l'image (nécessaire au calcul CSS `background-size`), jamais la taille du sous-segment.
- Stats de combat affichées (⚔️/🎯) toujours recalculées via `calculerAttaqueAffichee`/`calculerPreciseAffichee` (jamais `entite.baseA`/`baseP` bruts) dès qu'un pacte ou une synergie change un dégât de façon inconditionnelle — sinon le joueur ne voit jamais le bonus agir. `calculerAttaqueAffichee` accepte en plus les actions en attente du joueur pour prévisualiser en direct le bonus temporaire de la Synergie Guerrier (+2 dégâts par Défense déjà posée dans la file, avant même de valider le tour).
- ⚠️ **Une bulle ouverte au `:hover` seul est inaccessible sur téléphone, et un `title=` natif ne s'y affiche JAMAIS.** Toute explication de ce genre passe donc par `InfoBulle.tsx` (+ `hooks/useAppuiLong.ts`), qui double le survol d'un **appui long** (450 ms, annulé au-delà de 10px de déplacement pour ne pas confisquer le défilement) : la classe `.info-ancre--ouverte` expose la bulle, que le CSS montre aussi sur `:hover`. Au doigt il n'existe aucune « sortie de survol », la bulle reste donc affichée jusqu'au prochain appui ou défilement (écouteurs sur `window`). Le geste est réservé à `pointerType !== 'mouse'`, et `user-select`/`-webkit-touch-callout`/`onContextMenu` sont indispensables — sans eux le navigateur ouvre sa propre bulle de sélection par-dessus.
  - ⚠️ **Le placement dépend de l'endroit de l'écran** : `sens="bas"` pour ce qui est collé au sommet (badges de l'entête de combat), `alignement="fin"` pour ce qui vit à droite. `right: 0` fait croître la bulle vers la GAUCHE, donc elle ne peut jamais dépasser son ancre — c'est ce qui évite tout débordement horizontal sur mobile (vérifié à 370px : bulle à 366px). Les stats ⚔️/🎯/🛡️ gardent `haut`/`debut`, la carte du joueur étant la plus à gauche de l'arène.
  - ⚠️ **Un alignement fixe ne suffit pas à garder la bulle dans l'écran** : une ancre au milieu d'une carte étroite (les cases d'action sur téléphone) la fait sortir d'un côté ou de l'autre selon sa position, et le texte est alors coupé. `InfoBulle` mesure donc à l'ouverture et applique un `translateX` correctif (`recadrerDansEcran`), en raisonnant sur la position NON décalée pour ne pas empiler les corrections. Mesuré à 370px : une bulle qui irait à 509px est ramenée à 362px. Le recentrage à gauche passe après celui de droite — sur un écran plus étroit que la bulle, mieux vaut perdre la fin du texte que son début.
  - Tout ce qui portait un `title=` natif est passé par `InfoBulle` : cases d'action (gel/dérèglement), compteurs 🔥/🧪, badges de Pactes et de Bénédiction. Une case sans repère du Froid reste un simple `div` — inutile d'ancrer une bulle vide.
- Détail du calcul au survol des stats ⚔️/🎯/🛡️ du joueur (`utils/detailStats.ts` + `StatDetail.tsx`) : reconstruit base/arbre de compétence/Zone de Repos/pactes/synergies ligne par ligne. Comme aucun Pacte ne modifie `baseA`/`baseP` directement (seuls les multiplicateurs runtime type Puissance Brute/Ombre le font), tout écart entre `baseA` et (base + arbre) est imputé à la Zone de Repos sans avoir besoin de le tracker séparément. Bulle positionnée en `left:0` (pas centrée) car la carte du joueur est toujours la plus à gauche de l'arène — évite qu'elle déborde de l'écran sur mobile.
- Tutoriel d'introduction (Chat Mystérieux, `utils/tutoCombat.ts`, sprites `public/sprites/chat/`) : se lance automatiquement AVANT le Hub, une seule fois dans la vie du joueur (drapeau `tutoIntroFait` posé dès le lancement, pas à la fin, pour ne jamais se redéclencher même en cas d'abandon ; garde-fou supplémentaire pour les sauvegardes antérieures à cette fonctionnalité — drapeau absent — qui ne se déclenche que si le joueur n'a strictement aucune progression). PNJ intuable (12000 PV, esquive 100%) dont les actions sont scriptées tour par tour (`ACTIONS_CHAT_TUTO`, 5 tours fixes : Attaque, Précise, Défense, Esquive, Combo) plutôt que tirées aléatoirement, avec du dialogue injecté dans le log combat avant chaque tour (`DIALOGUE_CHAT_TUTO` — le tour 1 explique aussi les boutons Vitesse ⚡/Mode Auto-Manuel en haut de l'écran, avant même la première action). **Un seul bouton d'action reste actif par tour** (`actionsAutoriseesTuto`/`ACTIONS_AUTORISEES_TUTO`, greyé via le `.btn-action:disabled` déjà existant) — c'est le JOUEUR qui pratique chaque mécanique lui-même (jamais le Chat qui la démontre à sa place), ex: tour Combo = le joueur martèle sa propre Attaque et voit sa valeur grimper dans le log, pas le Chat. `CombatArene` accepte pour ça `estTutoriel`/`actionsMonstreScriptees`/`actionsAutoriseesTuto`/`dialogueTuto`/`onFinTutoriel`, qui court-circuite le flux victoire/défaite normal (mort du joueur ignorée pendant le tutoriel) pour terminer après le nombre de tours scriptés. Conclut sur une révélation de lore (`TutoConclusion.tsx`, texte complet à l'écran plutôt que dans le log combat qui disparaît au changement d'écran) expliquant le don du Pacte et la Vie Éternelle (diégétique pour la résurrection au Hub déjà existante), puis octroie un "Pacte de Niveau 0" purement cosmétique (`aPacteChat`, encart non-togglable dans l'Inventaire, aucune intégration mécanique).
- Équipement en un clic d'une synergie découverte (`composerEquipementSynergie` dans `utils/synergies.ts`, boutons en tête de l'Inventaire) : une synergie exige 4 Pactes répartis sur 3 emplacements de Niveau I + 1 de Niveau II, donc **exactement un** des quatre doit être porté en Niveau II. Un Pacte possédé UNIQUEMENT en Niveau II est prioritaire pour cet emplacement unique (sinon il n'entre nulle part) et deux Pactes dans ce cas rendent la synergie inéquipable — la fonction renvoie alors `null` et aucun bouton n'est proposé, plutôt qu'un raccourci qui échouerait en silence.
- Indice visuel de synergie dans l'Inventaire (`pactesManquantsPourSynergie` dans `utils/synergies.ts`) : dès que le joueur équipe 3 des 4 Pactes requis par une synergie cachée, le 4e Pacte (s'il est déjà possédé) reçoit la classe `pacte-carte--suggere` (halo doré pulsant, `@keyframes illumination-synergie` dans `App.css`) + badge "Synergie possible !" dans `Inventaire.tsx`. Ne révèle jamais LAQUELLE des 4 synergies est visée (conforme au principe "synergies = secret à découvrir") — se contente de désigner le Pacte manquant.
- Marqueur « Nouveau » sur les Pactes arrachés depuis la dernière visite de l'Inventaire (`pactesNonVus = pactesDebloques.slice(pactesVus)` — la liste étant toujours complétée par la fin, la coupure au compteur suffit, pas besoin d'une 2e clé). ⚠️ **`marquerPactesVus()` doit être appelé à la SORTIE de l'Inventaire, pas à son ouverture depuis le Hub** : les deux `setState` étant groupés par React, marquer à l'entrée vide la liste avant le premier rendu et aucun badge n'apparaît jamais.
- Inventaire : chaque section (Niveau I / Niveau II) n'affiche la liste COMPLETE de son niveau (`Object.keys(PACTES_REGISTRY)`, source de vérité unique) qu'une fois que le joueur possède au moins un Pacte de ce niveau (`possedeLvl1`/`possedeLvl2` — sinon la section entière reste masquée, pas seulement les cartes). Une fois affichée, les Pactes non débloqués de la section apparaissent grisés (`.pacte-carte--verrouille`, `opacity`+`grayscale`, non cliquable) avec leur nom mais sans leur effet (`???` à la place de la description). Les deux niveaux sont gérés indépendamment (posséder un Pacte de Niveau I ne révèle pas la section Niveau II, et vice-versa).
- Sauvegarde cloud Supabase (`utils/supabaseClient.ts`, `utils/sauvegardeCloud.ts`, câblée dans `useGameState.ts`) : `localStorage` reste la source de vérité immédiate, Supabase devient un miroir asynchrone identifié par un utilisateur anonyme (`signInAnonymously`, aucun compte/mot de passe côté joueur). Un instantané JSON des ~35 clés `tdp_*` gérées par `useGameState` (progression + méta-progression + stats de run) est poussé en `upsert` vers la table `sauvegardes` (RLS stricte sur `auth.uid() = user_id`) après une accalmie de `DELAI_DEBOUNCE_MS` (3s), plafonnée à `DELAI_PLAFOND_MS` (30s) — sans ce plafond, les dépendances de l'effet changeant à chaque tour de combat, un joueur rapide réarmerait le minuteur sans fin et ne sauvegarderait jamais. Plus un flush best-effort sur `visibilitychange`. ⚠️ **`setCloudUserId` doit impérativement rester APRÈS la tentative de restauration** : c'est lui qui arme l'autosauvegarde, et le poser avant l'appel réseau laisse un réseau lent écraser la sauvegarde cloud avec l'état local encore vide (perte de progression). L'horodatage `mis_a_jour_le` n'est pas envoyé par le client : un déclencheur Postgres l'impose côté serveur. La colonne `version` porte `APP_VERSION` (`utils/versionApp.ts`) et une sauvegarde d'une autre version est **refusée** à la restauration — sans ce refus, la purge locale déclenchée par un incrément de version serait aussitôt annulée par la restauration cloud, qui réinjecterait les données obsolètes. Exclut volontairement la clé `tdp_combat_actif` de `useCombatResume.ts` (écriture à chaque tick de combat, trop bavard et purement éphémère). ⚠️ **Ajouter un `useLocalStorage` dans `useGameState` sans inscrire sa clé dans `CLES_JEU` ne casse rien et ne lève rien** : le tableau de dépendances déclenche bien une sauvegarde, mais la donnée n'est jamais poussée — ça ne se voit qu'à la restauration sur un autre appareil. `sauvegardeCloud.test.ts` compare les deux listes et fige les exclusions assumées ; il lit le source via l'import `?raw` de Vite (la config TS de l'app ne charge pas les types Node). Au tout premier lancement sur un appareil dont l'état local est "vierge" (même garde-fou que le déclenchement du tuto), si une sauvegarde cloud existe déjà pour cette session anonyme, elle est restaurée dans `localStorage` puis la page est rechargée (plus simple/sûr que d'appeler individuellement les ~30 setters du hook). Limite connue acceptée pour cette v1 : l'identité anonyme vit elle-même dans le `localStorage` du navigateur, donc ça protège contre une perte locale (storage corrompu/vidé, purge de version) mais pas contre un vrai changement d'appareil sans avoir jamais eu de session commune — évolution possible plus tard via un upgrade vers un compte email (fonctionnalité native de Supabase Auth, pas de migration de schéma nécessaire).
- Protection Cloudflare Turnstile sur l'inscription anonyme (`utils/turnstile.ts`, script chargé dans `index.html`, jeton passé à `signInAnonymously` depuis `assurerSessionAnonyme`) : évite l'abus de création de sessions anonymes qui gonflerait la base/les quotas MAU gratuits (recommandation du dashboard Supabase). Mode "Invisible" côté Cloudflare = aucun widget visible pour un visiteur légitime. Le jeton n'est demandé qu'à la toute première création de session par navigateur (jamais au retour d'un joueur déjà connu, cf. le court-circuit sur `getSession()` existant). Nécessite `VITE_TURNSTILE_SITE_KEY` (clé publique, `.env.local`/Vercel) + la clé secrète correspondante configurée côté Supabase (Authentication → Attack Protection → Captcha). Dégradation gracieuse si absent/échoue : `signInAnonymously` est quand même tenté sans jeton (Supabase le rejette lui-même si la protection captcha est active côté projet — la sauvegarde cloud reste alors simplement inactive pour ce joueur, le jeu continue de fonctionner en localStorage pur). ⚠️ **Toute attente sur Turnstile doit rester bornée** (`DELAI_MAX_SCRIPT_MS` / `DELAI_MAX_TOKEN_MS`) : le script vient d'un tiers que bloqueurs de pub et réseaux d'entreprise coupent volontiers, et sans échéance la promesse ne se résout jamais — `assurerSessionAnonyme` reste suspendue et la sauvegarde cloud meurt en silence, sans erreur remontée.
- Bénédiction du Chat (`utils/benedictions.ts`, source unique de vérité `BENEDICTIONS_REGISTRY`) : le Chat Mystérieux réapparaît à la sortie de l'écran de fin de la **première run achevée** (`gererQuitterFin` → `ecran-benediction`, quelle que soit l'issue — texte railleur en cas de mort, admiratif en cas de victoire totale, mais même récompense). Il offre ensuite une **Roue de la Chance** (`ecran-roue`, `RoueChance.tsx`) jouée à CHAQUE entrée dans la Tour, qui tire un des 6 bonus pour toute l'ascension : Grâce Féline (+5% esquive **plat**, actif dès le palier 0 contrairement au Pacte de l'Esquive), Griffe Acérée (10% de critique x1.5), Pelage d'Acier (+10 Armure en début de tour — réutilise `regen_armure_tour`, le champ des passifs de boss, côté joueur), Regard Hypnotique (-25% d'esquive adverse), Vie de Chat (relève le joueur à 50% des PV une fois par run) et Leçon du Maître (XP x2).
  - Le tirage est fait **avant** `gererLancerRun` (il modifie les stats du héros), d'où le passage en deux temps `gererDemarrerAscension` → écran de la Roue → `gererLancerRun(benediction)`.
  - Vie de Chat est consommée dans `CombatArene` **avant** les branches de fin de combat, pour qu'un double KO se règle aussi en faveur du joueur (il se relève, l'adversaire reste mort).
  - Vie de Chat déclenche un **écran de résurrection bloquant** (`.vie-chat-ecran`, overlay dans `CombatArene`) que le joueur doit acquitter — jamais un simple log, qui passerait inaperçu au mode Auto en x4. C'est un overlay et non un `Ecran` à part : la résolution du tour est suspendue en plein vol (promesse résolue par le bouton, même mécanique que le pas-à-pas manuel), donc démonter l'arène perdrait l'état du combat.
  - Les 4 synergies apparaissent **grisées** dans les Archives dès le 1er Pacte de Niveau II arraché (`possedeLvl2` dans `Tuto.tsx`, classe `.tuto-carte--verrouille` calquée sur `.pacte-carte--verrouille`) : nom de la synergie visible, titre et effet masqués (`???`). Ce seuil n'est pas arbitraire — une synergie exige 4 Pactes équipés alors qu'il n'y a que 3 emplacements de Niveau I, donc un Pacte de Niveau II est la vraie condition d'entrée.
  - `calculer_degats` (Rust) prend désormais l'**attaquant entier** au lieu de ses drapeaux un par un : c'est lui qui porte le doublage de la Précise, la neutralisation d'esquive ET la réduction d'esquive adverse. La chance d'esquive réelle est centralisée dans `chance_esquive()` (`combat.rs`), mirorée côté TS par `calculerPaliersEsquiveAffiches(entite, adversaire)` — sans ce miroir le % affiché mentirait, même bug que `calculerAttaqueAffichee`. ⚠️ Le 2e paramètre est **l'adversaire entier**, pas sa seule réduction : il porte AUSSI `bloqueEsquiveOpposant`, et l'oublier laissait l'écran annoncer 100 % d'esquive face au Vent Mortel forme finale — le pire mensonge possible, puisqu'il pousse à jouer l'action qui ne peut pas réussir. Le passage d'un nombre reste toléré pour la réduction seule.
- **4 étages ajoutés (Froid, Foudre, Feu, Poison)** — la Tour compte 12 étages, donc 48 salles par run, et le Gardien Absolu monte à 1200 PV. Chaque Gardien donne un Pacte qui retourne sa mécanique contre les monstres (`PACTES_REGISTRY`).
  - **Froid** (`actions_resolution_inversee`, `actions_gelees`) : casse le postulat du simultané. Sur N créneaux tirés au hasard, le porteur agit AVANT l'autre — une Défense programmée sur ce créneau arrive trop tard. Le lvl1 gèle une action à la place (annulée : ni garde, ni dégâts, ni avancée de combo), le lvl2 cumule les deux. ⚠️ Un pouvoir porté des **deux** côtés s'annule : seul l'écart compte.
  - **Foudre** (`multiplicateur_degats_si_armure`) : x1,5/x2/x3 sur les dégâts **totaux** tant que la cible porte de l'armure — Précise comprise, qui garde par ailleurs son contournement. Se défendre contre lui est un piège.
  - **Feu** (`multiplicateur_brulure` → `brulure_active`) : ⚠️ l'**Attaque** (et elle seule) cesse de blesser directement — elle devient une brûlure qui **s'accumule** coup après coup (`+=`, pas un rafraîchissement) et se résout en fin de tour, absorbée par l'armure restante puis divisée par deux. Les occupants de l'étage n'ont pas la Précise (`kit_sans_precise`) : toutes leurs offensives passent donc en brûlure.
  - **Poison** (`multiplicateur_poison` → `poison_actif`) : miroir exact — la **Précise** (et elle seule) devient du poison. Traverse l'armure et ne décroît jamais. Les doses **s'additionnent dans le tour** (accumulateur `poison_sur_*` dans `jouer_tour`) **comme d'un tour à l'autre** (10 au tour 1 puis 10 au tour 2 = 20) : c'est ce qui force à conclure vite. Les occupants de l'étage n'ont pas l'Attaque (`kit_sans_attaque`).
  - ⚠️ **Brûlure et poison valent une PART des dégâts RÉELS de l'action** (`round(valeur * multiplicateur)`, où `valeur` sort de `get_valeur_action` : combos, Pacte du Temps et critique compris), jamais un forfait. `multiplicateur_brulure`/`multiplicateur_poison` sont des **`f32`** : **0.5 au Pacte Niveau I, 1.0 au Niveau II**, et 1.0 pour les créatures des deux étages. À 1.0 dès le Niveau I, le Pacte serait strictement supérieur à l'action qu'il remplace (brûlure qui s'empile, poison qui ignore l'armure) — d'où la conversion partielle.
  - Brûlure comme poison sont **esquivables** : la conversion est court-circuitée par `degats.esquive`. Elle ne peut pas dépendre de dégâts non nuls, sinon le Feu — qui n'en inflige plus — ne poserait jamais sa brûlure.
  - ⚠️ **Les tics de fin de tour sont HORS de la garde « les deux sont vivants »** : tuer le Gardien du Feu n'annule pas ce qu'on a déjà dans les veines, et le tic peut emporter le vainqueur (la branche double KO du `CombatArene` s'en charge). C'est ce qui a remplacé l'ancien `solderBrulure()` côté TS, supprimé. Les deux états sont ensuite nettoyés entre deux salles (`useGameState`), ils ne se transportent jamais d'un combat au suivant.
  - ⚠️ **Les créneaux du Froid sont tirés côté TS** (`tirerCreneauxFroid` dans `utils/combat.ts`) **à l'ouverture du tour**, pas dans le moteur, et passés à `jouer_tour` en 6e argument (`CreneauxFroid`) : le joueur doit voir les cases concernées PENDANT qu'il programme, sinon l'information arrive trop tard pour être jouable. Elles sont aussi renvoyées dans `ResultatTour` (`creneaux_geles_*`, `creneaux_*_dabord`) pour le marquage des cases (`.action-slot--gelee` / `--premier`).
  - ⚠️ **Les créneaux du Froid font partie de l'état du TOUR**, pas d'un état d'affichage : ils sont persistés par `useCombatResume` au même titre que les actions. Sans ça, recharger la page en plein tour les effaçait et le tour se résolvait sans le moindre effet de Froid. Le `??` à la lecture couvre les combats sauvegardés avant l'ajout du champ.
  - ⚠️ **Une action gelée fait quand même redescendre le palier d'esquive** (`subir_gel()`), comme n'importe quelle action qui n'est pas une Esquive. Elle ne monte aucune garde, mais sans cette décroissance se faire geler PROTÉGERAIT le palier au lieu de le coûter : trois Esquives puis un gel laissaient le joueur à 100% au lieu de 75%.
  - Une action **gelée** ne casse pas le combo mais ne le fait pas avancer : le moteur saute son `gerer_combo`, et **le compteur d'affichage de `CombatArene` doit sauter avec lui** (il lit `creneauxGeles*`) — sinon l'écran annonce un palier que les dégâts ne suivront pas.
  - ⚠️ **`corrigerActionsPourLimiteCombo` tire son action de remplacement dans `actionsPossibles`**, jamais dans la liste des 4 actions. Elle ne se déclenche que quand le JOUEUR porte le Pacte de la Fluidité (qui brise le combo adverse), ce qui rend le défaut rare et déroutant : c'est ainsi que le Brasier Vorace sortait une Précise qu'il ne possède pas. Une créature réduite à une seule action garde son combo, faute d'alternative.
  - ⚠️ **Aucune créature ne peut passer un tour entier sans action offensive**, à une seule exception : l'Étage du Poison une fois sa dose installée au-delà de `peut_temporiser_si_poison_depasse` (= 10, posé par `autoriser_temporisation()` dans `boss_data.rs`). Le critère est le **poison porté par la cible**, pas le numéro de tour — d'où `genererActionsMonstre(monstre, poisonSurCible)`, que `CombatArene` alimente avec `joueur.poisonActif`. Partout ailleurs le garde-fou remplace une action au hasard par une ⚔️/🎯, mais n'invente rien pour un kit sans offensive.
  - ⚠️ **Le soin du Pacte de la Vie II ne dépend que de la survie du JOUEUR** (`resultat.joueur.pv > 0`) : tuer l'ennemi au tour 5 ne doit pas l'annuler.
  - ⚠️ `appliquerFormeMegaBoss` emprunte aussi **`actionsPossibles`** : sans ça le Gardien Absolu gardait son kit complet et sortait une Précise sous les traits du Brasier Vorace (qui n'en a pas), ou une Attaque sous ceux de la Sève Noire — dont toutes les offensives doivent être du poison.
  - `appliquerFormeMegaBoss` doit reprendre les pouvoirs de ces 4 étages comme les autres — mais **jamais** `brulureActive`/`poisonActif`, qui sont des états subis par le Gardien Absolu lui-même et doivent survivre à ses changements d'apparence.
  - ⚠️ **Une action convertie ne produit AUCUN dégât direct** : sans ligne de journal dédiée, elle ne déclenchait ni la branche « esquive » ni la branche « dégâts » et le tour restait muet — impossible de savoir si le coup avait porté. D'où `brulure_posee`/`poison_pose` dans `ResultatDegats`, et une branche de log par cas. Toute nouvelle forme de conversion doit ajouter la sienne.
  - `ResultatDegats.chance_esquive` porte le taux appliqué au coup (réussi ou non), rendu par `mention_esquive()` — muet à 0 %. C'est ce qui rend lisible le vrai gain du Froid : un créneau déréglé frappe **avant** que la garde adverse ne se lève, donc contre le palier précédent (0/50/75/100 % au lieu de 50/75/100/100 %). Il ne contourne pas l'esquive.
  - `calculerAttaqueAffichee`/`calculerPreciseAffichee` appliquent aussi `multiplicateurBrulure`/`multiplicateurPoison` : la stat doit montrer **la dose posée**, pas des dégâts qui ne seront jamais infligés (11 d'Attaque sous Pacte du Feu I → `🔥 6`). ⚠️ L'ordre est celui du moteur — Foudre PUIS doublage de l'Ombre. Une action convertie se calcule sur la valeur **brute** : la Foudre n'entre pas dans une dose (elle vit dans `calculer_degats`, dont l'action convertie ressort à zéro), mais le **doublage de l'Ombre est repris explicitement** dans `convertir_en_poison` — sans cette reprise, associer l'Ombre et le Poison désactivait purement et simplement l'Ombre. La dose étant déjà entière, ce ×2 reste exact.
  - ⚠️ **Une cible immunisée (`part == 0.0`) ne se contente pas de ne plus rien encaisser : `tics_de_fin_de_tour` DISSIPE la réserve déjà accumulée** (et le journal le dit). C'est ce qui donne son sens au changement de forme du Gardien Absolu — passer en Brasier Vorace éteint le brasier allumé sur lui, passer en Sève Noire purge son poison, alors que ces deux états survivent normalement à ses changements d'apparence. Sans cette purge, l'immunité n'aurait fait que repousser l'échéance d'un tour. La forme étant appliquée en tête de tour (`CombatArene`), elle couvre bien la pose ET le tic.
  - **Résistance élémentaire des deux Gardiens** (`part_brulure_subie`/`part_poison_subi`, `Option<f32>` : absent = tout, 0.5 = résistant sur la forme évoluée, 0.0 = immunisé sur la finale) : ⚠️ elle s'applique **à la pose de la dose** (`appliquer_resistance()` dans `lib.rs`, mirorée par `appliquerResistance` dans `utils/combat.ts`), jamais au tic de fin de tour — sinon le compteur 🔥/🧪 annoncerait une dose que la cible ne subira pas, et la stat 🔥/🧪 du joueur mentirait du même facteur. D'où `ResultatDegats.dose_resistee` : une cible immunisée pose une dose **nulle**, donc aucune des branches de log existantes (« esquive », « dégâts », « dose posée ») ne se déclenchait et l'action disparaissait du tour. `mention_resistance()` rédige les deux cas.
  - ⚠️ **La Foudre est la seule contribution qui dépend de la CIBLE**, d'où le paramètre `cible` de ces deux fonctions (et de `detailAttaque`/`detailPrecise`). `cibleSeraArmee()` compte aussi `regenArmureTour` : l'armure du « Pelage d'Acier » n'est créditée qu'au lancement de la résolution, mais elle tombera à coup sûr — sans cette anticipation la stat ment pendant toute la phase de programmation.
  - ⚠️ **La conversion Feu/Poison a lieu dans `jouer_tour` AVANT les multiplicateurs de tour** (`convertir_valeur`, juste après `get_valeur_action`) : à partir de là `val_j`/`val_m` ne sont plus des dégâts mais une **dose**, et Ninja/Temps/critique portent dessus. L'arrondi tombe donc au même endroit que celui de `calculerPreciseAffichee` — sans quoi la stat annonce `🧪 4` et la 5e action doublée en pose 7 (`round(7*2*0.5)`) au lieu de 8. `convertir_en_*` ne fait plus que poser la dose reçue.
  - ⚠️ **Sur un créneau déréglé, un coup fatal empêche la riposte** (`if !gele_m && monstre.pv > 0`, et son symétrique). Sans cette garde le dérèglement n'était qu'un ordre d'affichage : on tuait son adversaire et on encaissait quand même son attaque. En **simultané**, au contraire, le double KO reste voulu — ne pas y ajouter la garde.
  - `ResultatDegats.valeur_appliquee` porte la valeur RÉELLEMENT infligée (Foudre, doublage de l'Ombre, ou dose de conversion). C'est elle que la jauge de combo du journal affiche : la valeur brute mentait d'un facteur 1,5 à 3 sur l'Étage de la Foudre. Elle vaut 0 sur une Défense/Esquive, où l'on retombe sur la valeur brute.
  - Le **Pacte du Temps** et le **Pacte du Combo** agissent déjà sur brûlure et poison sans code dédié : `convertir_en_*` reçoit `val_j`, qui a traversé `get_valeur_action` (combo) puis les multiplicateurs du Temps. Ne pas « corriger » ça en les réappliquant.
  - **L'icône de l'action suit son porteur** : `symbolePour(action, entite)` (`utils/combat.ts`) rend 🔥 pour l'Attaque d'un porteur de Feu et 🧪 pour la Précise d'un porteur de Poison — boutons, cases d'action, stats, jauge de combo. Mirorée par `symbole(act, entite)` dans `lib.rs` pour le journal. Ne jamais réintroduire `SYMBOLES[action]` en dur sur un élément lié à une entité : l'action ne blesse plus directement, l'icône doit le dire.
  - ⚠️ **`EtapeCombat` transporte les états différés** (`joueur_brulure`/`joueur_poison`/`monstre_*`) : sans eux l'interface ne peut lire brûlure et poison que sur l'entité finale, donc les compteurs 🔥/🧪 n'apparaîtraient qu'une fois le tour entièrement résolu — alors que c'est leur montée action par action qu'il faut suivre. Le poison exposé est `poison_affiche()` = `poison_actif + dose accumulée ce tour` (`poison_actif` n'est commité qu'à la fin du tour, mais les deux s'additionnent, donc la somme est exacte et ne redescend jamais en cours de tour).
  - `CreneauxFroid.annules` est **purement informatif** (créneaux où le dérèglement des deux camps s'est neutralisé, cf. `.action-slot--annulee`). `validerTour` ne transmet au moteur que les 4 autres champs — un tableau vide donnerait l'impression d'un Pacte du Froid inopérant à l'Étage du Froid, d'où ce marquage.
  - ⚠️ **Ordre imposé en fin de tour** : brûlure/poison d'abord, PUIS les assauts d'armure ("Pointes d'Acier" / Pacte de l'Armure II). Les flammes rongent la plaque avant qu'elle ne parte à l'assaut — vérifié : 155 d'armure moins 40 de brûlure = assaut de 115.
  - ⚠️ **Les deux assauts d'armure se résolvent sur le MÊME instantané**, pris avant tout échange. En séquence, celui qui frappait en second n'avait plus que le reliquat de son armure déjà entamé par l'assaut adverse : le résultat dépendait de l'ordre du code. Et l'armure lancée est **dépensée** — elle ne sert pas en plus de bouclier, donc deux porteurs face à face encaissent tout chacun de leur côté.
  - ⚠️ **`jouer_tour` sépare désormais le calcul des dégâts (`calculer_degats`) de leur application (`encaisser`)** : c'est ce qui rend l'ordre de résolution explicite. Les blocs de log ne mutent plus PV/armure, ils ne font que rédiger — y toucher en réintroduisant une soustraction créerait un double décompte silencieux.
- **Apparitions scénarisées du Chat entre deux runs** (`gererQuitterFin`, gabarit commun `ScenetteChat.tsx`) : à la sortie de l'écran de fin, une scène au maximum, toujours dans cet ordre et une seule fois chacune. 1) 1re run achevée → Bénédiction. 2) `runsTerminees >= 2` → `PresentationForgeron` (le Chat présente le forgeron, sourd, et explique l'Arbre). 3) run suivante → `LeconCombo` (rappel du Combo, la mécanique du tuto la plus vite oubliée car invisible pendant qu'on programme son tour). `runsTerminees` est incrémentée dans `capturerStatsFinRun`, appelée une fois par run **achevée** — un abandon ne compte donc pas.
  - **Leçons de mort** (`utils/leconsMort.ts`, `LECONS_MORT_REGISTRY`) : 4) une fois les 3 scènes ci-dessus passées, le Chat commente les morts causées par un pouvoir qu'on peut traverser sans le voir (Chronos, Mur de Fer, Vent Mortel, L'Anomalie, la Brute). La leçon retenue à la mort attend la sortie de l'écran de fin (`tdp_lecon_mort_attente`). Par défaut elle ne se joue qu'une fois à vie (`tdp_lecons_mort_vues`) — sauf celles marquées `rejouable` (Vent Mortel, Mur de Fer), répétées à chaque rechute. Le filtrage vit dans `leconAAfficher()`, séparé de `detecterLeconMort()` : la cause d'une mort ne dépend pas de l'historique du joueur, le droit de la commenter si.
    - Les conditions portent sur les **drapeaux de la forme affrontée**, jamais sur le nom du boss : la leçon du Vent Mortel n'existe que face à sa forme finale (seule porteuse de `bloqueEsquiveOpposant`), celle du Mur de Fer que face à la sienne, celle de L'Anomalie face aux formes évoluée et finale. Ajouter le drapeau à une autre forme y étendrait la leçon automatiquement, ce qui est le comportement voulu.
    - ⚠️ **Les circonstances sont déduites de l'ÉTAT, jamais du texte du journal** : `CirconstancesMort` est bâti dans `CombatArene` à partir des drapeaux de l'adversaire et des `etapes`. « Mort en fin de tour » = encore vivant à la dernière étape d'action mais mort dans `resultat.joueur` ; « s'est soigné » = PV du monstre qui remontent entre la dernière étape et le résultat. Recalculer ça depuis les intervalles (`regenPvChaqueXTours`) serait faux, le moteur sautant ses régénérations quand un camp est déjà tombé.
    - Chronos et le Mur de Fer exigent en plus `mortEnFinDeTour` : mourir sous leurs coups normaux n'apprend rien sur leur pouvoir. Le Gardien Absolu n'appartient à aucun étage, `gererFinMegaBoss` ne transmet donc aucune circonstance.
  - ⚠️ **Le bouton « Arbre de Compétences » du Hub est conditionné à `forgeronPresente`, pas à `xpTotal > 0`** : c'est le Chat qui ouvre la forge, même si le joueur a déjà des points à dépenser. Garde-fou pour les sauvegardes antérieures (clé `tdp_forgeron_presente` **absente** + progression existante → drapeau posé à `true` au démarrage), sinon un joueur en cours perdrait l'accès à un écran qu'il utilise déjà.
- **Tutoriel en écrans** (`LECONS_TUTO` dans `utils/tutoCombat.ts` + `LeconTutoEcran.tsx`) : chaque leçon s'affiche en overlay bloquant AVANT son tour, et le journal en garde une copie consultable pendant le tour. ⚠️ `DIALOGUE_CHAT_TUTO` est **dérivé** de `LECONS_TUTO` — un seul texte à maintenir. Overlay et non `Ecran` à part, pour la même raison que l'écran de Vie de Chat : le tour est en attente dans `CombatArene`, la démonter perdrait l'état du combat.
  - ⚠️ **`LECONS_TUTO` associe un TABLEAU de leçons à chaque tour**, pas une seule : le tour 1 en porte deux (les commandes Mode/Vitesse, puis l'Attaque). Une explication de l'interface n'a pas de tour à elle — elle ne se pratique pas — mais elle doit passer AVANT tout le reste, sinon une résolution en x4 défile plus vite qu'elle ne se lit et aucune des leçons suivantes n'est reçue. `CombatArene` suit donc deux compteurs : `leconVue` (dernier tour entièrement acquitté) et `leconIndex` (position dans le tour courant). La numérotation affichée est continue sur tout le tutoriel (`TOTAL_LECONS_TUTO`, `rangPremiereLeconDuTour`) — par tour, elle repartirait à 1 à chaque fois.
- Écran de félicitations à l'arrachage d'un Pacte (`PacteObtenu.tsx`, clé `tdp_pacte_obtenu`) : remplace la transition automatique vers l'étage suivant. Au tout PREMIER Pacte (`pactesDebloques.length <= 1`), il ajoute le conseil du Chat sur l'Inventaire — le joueur n'a alors aucune raison de l'avoir ouvert.
- Rappel d'Inventaire au lancement d'une run sans aucun Pacte équipé alors qu'on en possède (`RappelInventaire.tsx`). ⚠️ **`gererDemarrerAscension` teste `forcer === true`, jamais `!forcer`** : elle est branchée directement sur un `onClick`, qui lui passe son évènement en 1er argument — toujours truthy, ce qui court-circuitait le rappel sans que rien ne le signale. La sortie « Partir sans Pacte » passe par `ScenetteChat.actionSecondaire`, rendue APRÈS le bouton principal (dans les children elle se lirait comme le choix par défaut).
- Bilan chiffré en fin de tour (`variation_pv()` dans `lib.rs`) : un tour se lit sur une dizaine de lignes éparpillées, sans total il faut tout additionner de tête. Les PV de départ sont photographiés en tête de `jouer_tour`, donc le bilan inclut les tics de fin de tour.
- `LIMITE_COMBO_PREMIERE_RUN` (= 4) : tant que `runsTerminees === 0`, les monstres ne peuvent pas enchaîner 5 fois la même action. Le plafond le plus sévère l'emporte entre celui-ci et le Pacte de la Fluidité porté par le joueur.
- Chat Mystérieux interactif au Hub (`ChatHub.tsx`) : il somnole en bas à gauche et, **au clic** (jamais au survol), traverse l'écran en courant avant de bondir sur le bouton « Commencer l'Ascension » — ce qui **lance la run** (`onLancer`), exactement comme si le joueur avait pressé le bouton. C'est donc un second chemin vers le départ, pas une décoration. La position d'atterrissage est **mesurée à l'exécution** (`getBoundingClientRect` du bouton, passée en variables CSS `--chat-x`/`--chat-y`) plutôt que codée en dur, car elle dépend de la largeur de l'écran et du nombre d'entrées du menu. Le chat se pose sur le **bord haut** du bouton avec 4px de recouvrement seulement : la zone cliquable et le libellé restent intacts.
  - ⚠️ Les feuilles de sprites du Chat (`public/sprites/chat/`) sont dessinées tournées vers la **GAUCHE** : elles servent d'abord au monstre de l'arène, placé à droite et donc face au héros. Tout usage où le chat va vers la droite (le Hub) doit passer `miroir` à `SpriteAnime`, sinon il court à reculons.
- En-têtes de sécurité HTTP dans `vercel.json` (`X-Frame-Options`, `nosniff`, `Referrer-Policy`, `Permissions-Policy` en vigueur ; CSP volontairement en **`Report-Only`** pour l'instant). La CSP doit conserver `'wasm-unsafe-eval'` dans `script-src` (le moteur WASM ne s'instancie pas sans), `https://challenges.cloudflare.com` dans `script-src`/`frame-src` (Turnstile), `https://*.supabase.co` + `wss://*.supabase.co` dans `connect-src`, et `'unsafe-inline'` dans `style-src` (styles inline calculés à l'exécution, cf. conventions). Basculer en `Content-Security-Policy` (sans `-Report-Only`) une fois vérifié qu'aucune violation n'apparaît en console sur le déploiement.
- Journal des runs achevées (`utils/telemetrieRuns.ts`, table `public.runs`) : **un seul INSERT par run achevée**, déclenché depuis `capturerStatsFinRun` (`useGameState.ts`) qui reçoit désormais l'issue (`'mort' | 'victoire'`) en paramètre. Volontairement `void` (jamais attendu) — l'écran de fin ne doit pas dépendre du réseau. Répond à ce que `public.sauvegardes` ne peut pas dire : ce dernier ne porte qu'un état courant écrasé à chaque envoi, et **remis à zéro par tout incrément d'`APP_VERSION`** (purge locale puis réécriture d'un instantané vierge), donc l'historique d'équilibrage disparaît précisément quand on veut comparer avant/après.
  - La clé primaire `(user_id, numero_run)` porte toute la robustesse : elle déduplique un double envoi ET borne le volume qu'un joueur peut produire par sa propre progression — c'est ce qui dispense d'un rate-limit face à un porteur de la clé publique. `numero_run` est relu dans `localStorage` (`tdp_runs_terminees` + 1), pas pris dans la closure.
  - `user_id` n'est **jamais transmis par le client** : la colonne le tire du JWT (`default auth.uid()`), donc impossible à usurper. Table en **ajout seul** — policies INSERT et DELETE uniquement, l'absence volontaire de SELECT/UPDATE les refusant par défaut sous RLS.
  - ⚠️ `telemetrieRuns.ts` charge `supabaseClient` en **import différé** (`await import`) : ce dernier construit son client au niveau module et lève si les variables d'environnement manquent, ce qui rendrait `construireEvenementRun` (fonction pure, testée) intestable par le seul fait d'importer le fichier.
  - `pactes` est **trié** à la construction : sans ça une même composition équipée dans deux ordres différents produit deux tableaux distincts, et un `group by pactes` la compte comme deux compositions — faux sans que rien ne le signale. `etages` au contraire n'est **jamais** trié : la Tour étant mélangée à chaque run, c'est l'ordre qui porte l'information (`etages[etage]` donne l'étage où la run s'est arrêtée).
  - ⚠️ **`numero_run` compte les runs LANCÉES** (`tdp_runs_lancees`, incrémenté dans `gererLancerRun`), pas les runs achevées : un abandon est journalisé mais n'incrémente pas `runsTerminees`, il consommerait donc le numéro de la run suivante et la clé primaire en rejetterait une des deux **en silence**. Le compteur démarre à `tdp_runs_terminees` (et non 0) pour ne pas réutiliser les numéros déjà journalisés chez un joueur installé.
  - Un **abandon** (`gererAbandon`) est journalisé mais ne passe pas par `capturerStatsFinRun` : il ne compte ni pour `runsTerminees` (qui cadence les apparitions du Chat) ni pour le record. Garde `listeEtages.length > 0` pour ne pas enregistrer de run fantôme.
  - Zones de Repos : **deux** compteurs de run, `tdp_repos_proposes_run` et `tdp_repos_pris_run`. Au-delà de la 1re visite d'une run seules 3 des 5 options sont tirées, donc un choix peu pris peut n'avoir été que peu **proposé** — sans le dénominateur, le taux d'utilisation reflète surtout la chance du tirage.
  - Actions et combos : `ajouterStatsTour` prend désormais un objet `StatsTour` (elle avait 3 arguments, elle en aurait 9). `CombatArene` réutilise le suivi de combo qu'il fait **déjà pour l'affichage** (`localComboJ`/`localComboM`), donc gel de l'Étage du Froid compris. Les actions programmées comptent les créneaux gelés (le joueur les a bien choisis) ; les combos ne les comptent pas (le moteur y saute le calcul de palier, il n'y a aucun palier à mesurer).
  - ⚠️ **Le combo est stocké en SOMME + NOMBRE, jamais en moyenne déjà calculée** (`combo_somme_*`/`combo_actions_*`) : une moyenne de moyennes est fausse dès qu'on agrège des runs de longueurs différentes, alors que `sum(somme)/sum(actions)` reste exact. Le combo des **monstres** est mesuré aussi : leurs actions étant tirées au hasard, leur moyenne est l'étalon de ce qu'on obtient sans chercher à enchaîner — c'est l'écart joueur/monstres qui dit si la mécanique est comprise.
- **Mode Hardcore** (`utils/hardcore.ts`, écrans `ChoixExtraction.tsx` / `HardcoreIntro.tsx`) : second profil de progression, débloqué à vie une fois le Gardien Absolu terrassé. Aucune règle de combat ne change — seule la **clé localStorage** sous laquelle la progression est rangée change (`CLES_PROFIL` → préfixe `tdp_hc_`), si bien que les deux profils coexistent sans se voir et qu'une mort en hardcore ne peut pas toucher la partie normale. La mort y efface le profil (Pactes, XP, compétences, record, Pactes victorieux) ; la seule parade est la porte de sortie offerte à la **fin de chaque étage, avant la Zone de Repos** (`ecran-extraction`), plus une dernière fois sur l'écran de Sortie de la Tour.
  - ⚠️ **Basculer de mode IMPOSE un rechargement de page** (`basculerProfil`) : `useLocalStorage` ne lit sa clé qu'une fois (initialiseur de `useState`), donc changer la clé en cours de session laisserait tous les états sur l'ancien profil tout en écrivant dans les clés du nouveau. C'est aussi pourquoi le setter de `modeHardcore` n'est volontairement pas récupéré du hook.
  - ⚠️ **L'écriture qui précède ce rechargement passe DIRECTEMENT par `localStorage`**, jamais par les setters : l'updater de `useLocalStorage` n'est exécuté que lors du rendu suivant, qui n'aura jamais lieu. C'est le même piège que celui décrit dans `utils/logs.ts`, dans l'autre sens.
  - ⚠️ **L'abandon compte comme une mort en hardcore** (`gererAbandon`) : sinon quitter par le menu au premier coup dur conserverait le butin que la porte de sortie fait payer, et la décision de fin d'étage n'aurait plus d'enjeu. Il passe donc par `capturerStatsFinRun` + `effacerProfilHardcore` comme une mort — mais **l'issue journalisée reste `abandon`**, renoncer et se faire tuer ne devant pas se confondre en analyse. `logsMort` est vidé (personne n'a porté de coup fatal, un « Coup de Grâce » raconterait une mort qui n'a pas eu lieu) et `Fin.tsx` a sa propre branche `estAbandon`. Le `window.confirm` de `CombatArene` reçoit `modeHardcore` pour annoncer la perte : le même bouton est sans conséquence dans l'autre mode, rien à l'écran ne distingue les deux.
  - ⚠️ **`effacerProfilHardcore()` est appelée APRÈS `capturerStatsFinRun`** : l'écran de fin doit encore pouvoir montrer ce que l'ascension avait accompli, record compris (que la remise à zéro annule juste après).
  - `tdp_tour_vaincue` est **partagée** entre les deux profils et distincte de `tdp_victoire` (qui ne décrit que l'issue de la DERNIÈRE run et repasse à `false` à la mort suivante). Garde-fou pour les sauvegardes antérieures : clé absente + (`victoireTotale` ou `pactesVictorieux` non vide) → posée à `true` au démarrage.
  - Tout ce qui n'est pas dans `CLES_PROFIL` est **partagé** délibérément : c'est du savoir, pas de la puissance (Synergies découvertes, bestiaire, Archives, tutoriel, progrès du Chat). `forgeronDisponible`/`benedictionDisponible` (`forgeronPresente || modeHardcore`, idem Bénédiction) donnent Forgeron et Roue d'emblée en hardcore, sans toucher aux drapeaux partagés que seules les scènes du Chat posent.
  - La synchro cloud pousse **les deux profils**, y compris celui qui dort (`CLES_PROFIL_HARDCORE` concaténée à `CLES_JEU`) — sinon une restauration faite depuis l'autre mode effacerait une progression entière.
  - ⚠️ **L'extraction doit vider `tdp_logs_mort`** : cette clé n'est écrite qu'à la mort et survit d'une run à l'autre, donc l'écran de fin afficherait le « Coup de Grâce » d'une mort précédente. `Fin.tsx` distingue les trois issues via `stats.issue` (`StatsRun.issue`, optionnelle pour les sauvegardes antérieures), jamais via le seul booléen `victoire`.
  - Journal cloud : colonne `hardcore` + issue `'extraction'` (migration `20260815120000_mode_hardcore.sql`). Sans le drapeau, les runs hardcore — profil reparti de zéro, donc bien plus faibles à étage égal — fausseraient tous les agrégats d'équilibrage.
- **Classement hardcore** (`utils/classement.ts`, table `public.classement`, écrans `ClassementSaisie.tsx` / `Classement.tsx`) : à la victoire hardcore, `gererFinMegaBoss` détourne vers `ecran-classement-saisie` avant l'écran de fin, où le joueur inscrit un nom. Le Hub expose `🏆 Classement` sous la même condition que le mode hardcore (`aVaincuLaTour`) — l'afficher plus tôt révélerait une fin de jeu non atteinte.
  - ⚠️ **Le score a son PROPRE compteur, `tdp_hc_runs`.** `tdp_runs_lancees`/`tdp_runs_terminees` ne sont **pas** dans `CLES_PROFIL` : ils sont partagés entre les deux modes et incluent donc toutes les runs normales jouées avant même le déblocage du hardcore. Les réutiliser donnerait un score sans rapport avec l'exploit. Ne pas non plus les ajouter à `CLES_PROFIL` : `tdp_runs_lancees` sert de clé primaire à `public.runs`, deux profils indépendants produiraient des `numero_run` en collision.
  - `tdp_hc_runs` est remis à zéro par `effacerProfilHardcore()` : la mort effaçant toute la puissance, « fini en N runs » veut dire « fini presque en partant de rien », et tout le monde garde une chance de battre le record. `tdp_hc_runs_totales` conserve le total à vie, hors classement.
  - ⚠️ **`public.classement` est la SEULE table lisible par tous** (policy SELECT `using (true)`) — c'est ce qui en fait un classement, alors que `sauvegardes` est cloisonnée et `runs` même pas relisible. Le `nom` est donc du contenu public : nettoyé côté client (`nettoyerNomJoueur`, unicode pour garder les accents) **et** contraint en base (longueur, `btrim`, refus de `[[:cntrl:]<>&]`), le client pouvant être contourné par un porteur de la clé publique. Il n'y a **aucun filtre d'insultes**.
  - Le record ne se dégrade jamais : un déclencheur `BEFORE INSERT OR UPDATE` renvoie `OLD` si `new.nb_runs >= old.nb_runs`. Sans lui, le client — qui a le droit d'UPDATE sur sa ligne — pourrait écraser son propre record, volontairement ou non. Le déclencheur impose aussi `obtenu_le`, qui départage les ex æquo.
- **Sprites propres à une créature** (`animationsPourMonstre` dans `animationsMonstre.ts`, table `SPRITES_DEDIES`) : tout ce qui n'y figure pas garde le champignon, qui reste le sprite par défaut de la Tour. La clé est un **fragment** de nom, les boss étant préfixés par leur forme côté Rust (`👑 BOSS: `, `👑FORME EVOLUEE: `…) — les trois formes doivent partager la même apparence. Le Gardien Absolu recopie le nom de la forme qu'il revêt (`appliquerFormeMegaBoss`), il en hérite donc le sprite : voulu, il change d'apparence à chaque tour. ⚠️ Un renommage dans `boss_data.rs` ferait retomber le boss sur le champignon **en silence**, d'où `animationsMonstre.test.ts` qui fige les noms réellement produits par le moteur.
  - Premier occupant : **Le Souffle Immobile** (Gardien du Froid), 5 feuilles générées par `outils/sprites/souffleImmobile.mjs`. Colosse au dos voûté, tête basse projetée en avant, poings au sol : son Attaque est un poing abattu qui fait jaillir des pointes de glace vers la cible, son Esquive un pas d'appui lourd avec rémanence, et sa mort un éclatement en morceaux découpés dans sa propre silhouette (partition de Voronoï sur les pixels dessinés).
  - ⚠️ **Trois règles de dessin ont fait toute la différence sur cette créature**, et vaudront pour la suivante : (1) chaque partie sur sa propre couche cernée (`couche()`), sinon les membres fusionnent en une tache unie ; (2) de l'**air** entre les membres — la lecture tient à l'alternance bras · vide · jambe · vide · jambe · vide · bras, un ventre trop bas la détruit ; (3) un **écart de valeur** par plan (bras avant plus clair que le torse, membres arrière en `creux`), le contour seul ne suffisant pas quand deux formes se touchent.
- ⚠️ **Turnstile refuse `127.0.0.1`** (erreur 110200, domaine non autorisé sur le widget Cloudflare) : en dev local, `signInAnonymously` est donc rejeté par Supabase (« captcha protection: request disallowed ») et **la sauvegarde cloud comme le journal de runs sont inactifs**, silencieusement. La prod (`tour-des-pactes.vercel.app`) n'est pas concernée, son domaine étant autorisé. Pour tester ces chemins en local, ajouter `127.0.0.1` et `localhost` aux hostnames du widget dans le dashboard Cloudflare.
